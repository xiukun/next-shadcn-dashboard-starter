## 阶段 1：列管理面板（Column Management UI）

### 1.1 核心状态扩展
- [x] 1.1.1 扩展 `DataGridViewState`，添加列顺序、宽度、可见性状态
- [x] 1.1.2 创建 `useColumnPersistence` Hook，实现 localStorage 持久化
- [x] 1.1.3 在 `useDataGrid` 中集成列状态恢复逻辑（含新增列自动补全逻辑）

### 1.2 列管理面板组件
- [x] 1.2.1 创建 `ColumnManagementPanel` 组件（使用 shadcn Drawer）
- [x] 1.2.2 实现列列表渲染（显示/隐藏 checkbox）
- [x] 1.2.3 实现列顺序拖拽（使用 @dnd-kit）
- [x] 1.2.4 实现列宽输入框/滑块
- [x] 1.2.5 实现列固定位置切换（左/右/无）
- [x] 1.2.6 添加"重置"和"应用"按钮

### 1.3 集成与测试
- [x] 1.3.1 在 `DataGrid` 组件中添加列管理入口按钮
- [x] 1.3.2 实现列状态变更的实时预览
- [x] 1.3.3 添加 i18n 翻译（zh/en）
- [x] 1.3.4 在 `table-demo` 页面测试列管理功能

## 阶段 2：列虚拟化 + Pinned 布局

### 2.1 列虚拟化引擎
- [x] 2.1.1 创建 `useColumnVirtualization` Hook
- [x] 2.1.2 实现列宽累积偏移量计算
- [x] 2.1.3 实现可见列范围计算（基于 scrollLeft）
- [x] 2.1.4 集成到 `DataGrid` 的列渲染逻辑（当列数 > 20 时自动启用）

### 2.2 Pinned 列布局
- [x] 2.2.1 重构 `DataGrid` 表格结构，拆分为三个区域（left/center/right）
- [x] 2.2.2 实现固定列的 `position: sticky` 样式（含正确的 z-index 层级）
- [x] 2.2.3 实现三个区域的表头同步滚动（通过统一的 scrollLeft 状态）
- [x] 2.2.4 处理固定列与滚动列之间的边界交互

### 2.3 列宽自动调整
- [x] 2.3.1 实现列边缘双击检测
- [x] 2.3.2 实现内容宽度测量（当前可见行）
- [x] 2.3.3 实现自动列宽计算与应用
- [x] 2.3.4 添加视觉反馈（拖拽手柄 hover 效果）

### 2.4 性能优化与测试
- [x] 2.4.1 测试 50+ 列场景下的滚动性能（已在 demo 中扩展到 40 列）
- [x] 2.4.2 测试 pinned 列在不同滚动位置的表现
- [x] 2.4.3 优化虚拟化窗口的 overscan 策略（默认 overscan: 2）
- [ ] 2.4.4 添加性能监控与调试工具（可选，暂未实现）

## 阶段 3：增强功能与修复（额外完成）

### 3.1 UI 增强
- [x] 3.1.1 添加 `showHeaderVerticalDividers` 属性，支持可配置的列头竖线显示
- [x] 3.1.2 优化固定列背景色，使用 `hsl(var(--muted))` 确保正确渲染
- [x] 3.1.3 修复固定列表头在垂直滚动时的 z-index 层级问题（表头固定列 z-30，数据单元格 z-20）

### 3.2 SSR/CSR 水合修复
- [x] 3.2.1 将 `table-demo` 中的 `DataGrid` 改为 `dynamic` 客户端渲染（`ssr: false`）
- [x] 3.2.2 优化 `useDataGrid` 中的列状态持久化加载时机，避免 SSR 水合冲突
- [x] 3.2.3 实现新增列的自动补全逻辑，避免旧持久化数据覆盖新列

### 3.3 Demo 扩展
- [x] 3.3.1 在 `table-demo` 中扩展到 40 列（5 基础列 + 35 扩展列）
- [x] 3.3.2 添加扩展列的 i18n 翻译支持（`columns.extra`）
- [x] 3.3.3 默认将 ID 列固定到左侧，便于测试固定列功能
