# 任务清单：修复 Tabs、图表和 API 路由相关问题

## Change ID

`20260220232925-fix-tabs-chart-api-issues`

## 任务状态说明

- ⏳ 待开始
- 🔄 进行中
- ✅ 已完成
- ❌ 已取消

## 任务列表

### 阶段 1：Tab 标题多语言修复

#### 任务 1.1：修改默认 Tab 标题
- **状态**: ✅ 已完成
- **文件**: `src/stores/route-tabs-store.ts`
- **内容**: 
  - 将 `DEFAULT_TAB.title` 从中文「仪表盘」改为英文「Dashboard」
  - 添加注释说明会在 `useRouteTabs` 中根据当前 locale 更新
- **验收**: 默认 Tab 标题为英文

#### 任务 1.2：优化 Tab 更新逻辑
- **状态**: ✅ 已完成
- **文件**: `src/hooks/use-route-tabs.ts`
- **内容**: 
  - 确保 Tab 已存在时也能更新标题和图标
  - 优化更新条件判断，避免无限循环
- **验收**: Tab 标题能根据当前 locale 正确更新

### 阶段 2：Tab 激活状态同步

#### 任务 2.1：修复 Tab 激活状态同步
- **状态**: ✅ 已完成
- **文件**: `src/hooks/use-route-tabs.ts`
- **内容**: 
  - 在路由变化时，确保 `activeTabId` 与当前 canonical path 一致
  - 优化更新逻辑，避免不必要的更新
- **验收**: Tab 激活状态与路由 URL 一致

### 阶段 3：图表容器尺寸修复

#### 任务 3.1：修复图表容器尺寸
- **状态**: ✅ 已完成
- **文件**: `src/components/ui/chart.tsx`
- **内容**: 
  - 在容器样式中添加 `width: '100%'` 和 `height: '100%'`
  - 为 `ResponsiveContainer` 添加明确的 `width` 和 `height` props
- **验收**: 图表容器尺寸警告消失，图表正常渲染

### 阶段 4：API 路由处理修复

#### 任务 4.1：修复 API 路由处理
- **状态**: ✅ 已完成
- **文件**: `src/proxy.ts`
- **内容**: 
  - 在中间件中跳过对 `/api` 和 `/trpc` 路由的 next-intl 处理
  - 确保 API 路由直接由 Next.js 处理
- **验收**: API 路由返回 200，不再出现 404 错误

### 阶段 5：验证与测试

#### 任务 5.1：TypeScript 类型检查
- **状态**: ✅ 已完成
- **命令**: `npx tsc --noEmit`
- **验收**: 无类型错误

#### 任务 5.2：功能验证
- **状态**: ✅ 已完成
- **内容**: 
  - 验证 Tab 标题多语言显示
  - 验证 Tab 激活状态同步
  - 验证图表容器尺寸
  - 验证 API 路由处理
- **验收**: 所有功能正常工作

## 完成标准

所有任务完成后，项目应满足：

1. ✅ Tab 标题多语言显示正确
2. ✅ Tab 激活状态与路由一致
3. ✅ 图表容器尺寸警告消失
4. ✅ API 路由正常工作
5. ✅ TypeScript 类型检查通过
