---
name: DataGrid组件
description: DataGrid 组件库开发规则
alwaysApply: true
---
# DataGrid 组件库开发规则

DataGrid 是一个基于 TanStack Table 和 Zustand 的企业级数据表格组件库，位于 `packages/maita-table-react/`。

### 架构原则

1. **Zustand Slice 模式**
   - ✅ 使用 Zustand Slice 模式管理状态，不要使用 Controller 模式
   - ✅ Store 分为 4 个独立的 slices：
     - `viewSlice` - 视图状态（排序、过滤、列配置）
     - `runtimeSlice` - 运行时状态（选择、编辑、提交）
     - `dataSlice` - 数据状态（行数据）
     - `paginationSlice` - 分页状态
   - ✅ 所有状态更新必须通过 slice actions，**禁止**直接使用 `store.setState()`
   - ✅ 使用 `createDataGridStore()` 和 `createInitialState()` 创建 store

2. **组件拆分原则**
   - ✅ `DataGrid.tsx` 应保持简洁（目标 200-300 行），只负责组合 hooks 和组件
   - ✅ 大型逻辑应提取到专用 hooks（如 `useTableInstance`、`useTableColumns` 等）
   - ✅ 渲染逻辑应拆分到独立组件（`DataGridHeader`、`DataGridBody`、`DataGridCell`、`DataGridPagination`）
   - ✅ 每个组件职责单一，易于维护和测试

3. **Hooks 组织**
   - ✅ 每个功能使用独立的 hook（如 `useColumnSorting`、`useColumnFiltering`、`useRowSelection` 等）
   - ✅ Hooks 必须使用 slice actions 更新状态，不要直接操作 store
   - ✅ 复杂逻辑应提取到专用 hooks（如 `useColumnResize`、`useEditableCellNavigation`、`useTableInteraction`）
   - ✅ Hook 命名遵循 `use[Feature]` 模式

### 状态管理规则

1. **Store 使用**
   ```typescript
   // ✅ 正确：使用 slice actions
   store.getState().setSort([{ id: 'name', desc: false }]);
   store.getState().setPageIndex(2);
   store.getState().setSelection(new Set(['1', '2']));

   // ❌ 错误：直接使用 setState
   store.setState({ view: { ...state.view, sort: [...] } });
   ```

2. **类型安全**
   - ✅ 使用 `DataGridStore<Row>` 类型，不要使用 `any`
   - ✅ 使用 `DataGridStoreState<Row>` 获取状态类型
   - ✅ 所有 slice actions 必须保持类型安全

3. **状态持久化**
   - ✅ 使用 `useColumnPersistence`、`useSelectionPersistence`、`usePaginationPersistence` hooks
   - ✅ 持久化逻辑应独立于业务逻辑
   - ✅ 使用 `localStorage` 存储状态，key 格式：`grid-{feature}-{gridId}`

### 组件开发规则

1. **组件拆分**
   - ✅ 表头逻辑 → `DataGridHeader.tsx`
   - ✅ 表体逻辑 → `DataGridBody.tsx`
   - ✅ 单元格逻辑 → `DataGridCell.tsx`
   - ✅ 分页逻辑 → `DataGridPagination.tsx`
   - ✅ 每个组件文件控制在 200-400 行

2. **Props 接口**
   - ✅ 使用泛型 `<Row>` 支持任意行数据类型
   - ✅ Props 接口命名为 `{ComponentName}Props<Row>`
   - ✅ 所有可选 props 必须提供默认值或明确标记为可选

3. **组件组合**
   - ✅ `DataGrid.tsx` 只负责组合，不包含复杂逻辑
   - ✅ 使用 hooks 管理状态和副作用
   - ✅ 使用子组件处理渲染

### Hooks 开发规则

1. **Hook 命名**
   - ✅ 功能相关：`useTable[Feature]`（如 `useTableInstance`、`useTablePagination`）
   - ✅ 列相关：`useColumn[Feature]`（如 `useColumnSorting`、`useColumnFiltering`）
   - ✅ 行相关：`useRow[Feature]`（如 `useRowSelection`）
   - ✅ 通用：`use[Feature]`（如 `useDataGrid`、`useSelectionPersistence`）

2. **Hook 职责**
   - ✅ 每个 hook 只负责一个功能领域
   - ✅ Hook 应返回清晰的接口（如 `{ handleSort, sortState }`）
   - ✅ 复杂逻辑应拆分为多个 hooks

3. **Hook 实现**
   - ✅ 使用 `useCallback` 包装事件处理函数
   - ✅ 使用 `useMemo` 缓存计算结果
   - ✅ 使用 `useEffect` 处理副作用（订阅、持久化等）
   - ✅ 所有 hooks 必须标记 `'use client'`

### 测试规则

1. **测试文件位置**
   - ✅ Store slices 测试：`tests/store/{sliceName}.test.ts`
   - ✅ Hooks 测试：`tests/hooks/{hookName}.test.ts`
   - ✅ 组件测试：`tests/components/{componentName}.test.tsx`

2. **测试要求**
   - ✅ 使用 `createDataGridStore()` 和 `createInitialState()` 创建测试 store
   - ✅ 使用新的 slice actions 进行状态操作，不要使用旧的 `setState` 或 `dispatch`
   - ✅ 测试异步行为时使用 `waitFor` 等待状态更新
   - ✅ 所有测试必须通过（100% 通过率）

3. **测试更新**
   - ✅ 重构时同步更新所有相关测试
   - ✅ 测试应覆盖主要功能和边界情况
   - ✅ 使用 `@testing-library/react` 进行组件测试

### 代码质量规则

1. **类型安全**
   - ✅ 避免使用 `any`，优先使用明确的类型
   - ✅ 使用泛型 `<Row>` 支持类型推断
   - ✅ 修复所有类型错误，不要使用类型断言绕过

2. **代码组织**
   - ✅ 统一导入路径：`from '../store'` 而不是 `from '../store/index'`
   - ✅ 清理未使用的导入
   - ✅ 删除临时文件和旧代码

3. **性能优化**
   - ✅ 使用 `useMemo` 缓存列定义和计算结果
   - ✅ 使用 `useCallback` 缓存事件处理函数
   - ✅ 使用虚拟化处理大量数据（`useTableVirtualization`）

### 分页功能规则

1. **分页模式**
   - ✅ 客户端分页：使用 `getPaginationRowModel()`，适合 < 10,000 行
   - ✅ 服务端分页：使用 `manualPagination: true`，必须提供 `rowCount` 或 `pageCount`

2. **分页状态**
   - ✅ 使用 `paginationSlice` 管理分页状态
   - ✅ `setPageSize` 会重置 `pageIndex` 为 0，注意调用顺序
   - ✅ 支持分页状态持久化（`usePaginationPersistence`）

3. **分页组件**
   - ✅ 使用 `DataGridPagination` 组件显示分页控件
   - ✅ 支持首页/上一页/下一页/末页按钮
   - ✅ 支持每页条数选择器

### 编辑功能规则

1. **编辑模式**
   - ✅ `immediate` - 立即提交
   - ✅ `single-row` - 单行编辑
   - ✅ `batch` - 批量编辑

2. **编辑状态管理**
   - ✅ 使用 `runtimeSlice` 管理编辑状态（`pendingEdits`、`editingCell` 等）
   - ✅ 使用 `useTableSubmission` hook 处理提交逻辑
   - ✅ 使用 `useEditableCellNavigation` hook 处理单元格导航

3. **验证**
   - ✅ 使用 Zod Schema 进行结构化验证
   - ✅ 列级验证：`meta.zodSchema`
   - ✅ 行级验证：`createRowSchema(columns)`

### 常见陷阱

1. **状态更新**
   - ❌ 不要使用 `store.setState()` - 使用 slice actions
   - ❌ 不要直接修改 state 对象 - 使用不可变更新
   - ✅ 使用 `store.getState().setSort()` 等 actions

2. **类型冲突**
   - ❌ 避免泛型参数名冲突（如 `Row` 与 TanStack 的 `Row` 类型）
   - ✅ 使用 `TRow` 或别名导入（如 `TanStackRow`）

3. **测试异步问题**
   - ❌ 不要使用固定的 `setTimeout` - 使用 `waitFor`
   - ✅ 使用 `act()` 包装状态更新
   - ✅ 注意 `setPageSize` 会重置 `pageIndex`，测试时注意调用顺序

### 文件结构

```
packages/maita-table-react/src/
├── DataGrid.tsx              # 主组件（200-300 行）
├── useDataGrid.tsx           # 数据获取和 store 初始化
├── store/                    # Zustand Store
│   ├── index.ts             # Store 创建和导出
│   ├── types.ts             # Store 类型定义
│   └── slices/              # Store Slices
│       ├── viewSlice.ts     # 视图状态
│       ├── runtimeSlice.ts  # 运行时状态
│       ├── dataSlice.ts     # 数据状态
│       └── paginationSlice.ts # 分页状态
├── hooks/                    # 自定义 Hooks
│   ├── useTableInstance.ts  # TanStack Table 实例
│   ├── useTableColumns.ts   # 列定义生成
│   ├── useTablePagination.ts # 分页逻辑
│   ├── useColumnSorting.ts  # 列排序
│   ├── useColumnFiltering.ts # 列过滤
│   ├── useRowSelection.ts   # 行选择
│   └── ...
└── components/               # 渲染组件
    ├── DataGridHeader.tsx   # 表头
    ├── DataGridBody.tsx     # 表体
    ├── DataGridCell.tsx     # 单元格
    └── DataGridPagination.tsx # 分页控件
```

### 开发检查清单

在修改 DataGrid 组件库时，确保：

- [ ] 使用 Zustand Slice 模式，不使用 Controller 模式
- [ ] 所有状态更新通过 slice actions
- [ ] 组件保持简洁，复杂逻辑提取到 hooks
- [ ] 所有类型安全，无 `any` 断言
- [ ] 测试已更新并通过（100% 通过率）
- [ ] 代码组织清晰，导入路径统一
- [ ] 性能优化（使用 `useMemo`、`useCallback`）
- [ ] 遵循现有代码风格和模式

